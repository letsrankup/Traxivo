module.exports = {
  plugins: {
    // 1. Tailwind CSS engine ko invoke karta hai
    tailwindcss: {},
    
    // 2. Browser compatibility ke liye vendor prefixes (jaise -webkit-, -moz-) automatically add karta hai
    autoprefixer: {},
  },
}

